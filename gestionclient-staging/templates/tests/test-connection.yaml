apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gestionclient-staging.fullname" . }}-test-connection"
  labels:
    {{- include "gestionclient-staging.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gestionclient-staging.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
